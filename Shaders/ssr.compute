// ssr.compute
// Compute shader for processing 
#version 430 core
layout(local_size_x = 32, local_size_y = 32) in;

layout(rgba8,   binding = 0) readonly uniform image2D inputImage;
layout(rgba32f, binding = 1) readonly uniform image2D inputWorldPos;
layout(rgba8,   binding = 2) readonly uniform image2D inputSpecular;
layout(rgba8,   binding = 4) uniform image2D outputImage;

layout(location = 0) uniform uint screenWidth;
layout(location = 1) uniform uint screenHeight;
layout(location = 2) uniform float stepSize;
layout(location = 3) uniform float sampleWeight;
layout(location = 4) uniform uint stepCount;
layout(location = 5) uniform mat4 viewProjection;
layout(location = 6) uniform vec3 camPos;
layout(location = 7) uniform int SSR_RES;

vec4 unpackColor32(uint color) {
    return vec4(float((color >> 24) & 0xFFu) / 255.0,  // r
                float((color >> 16) & 0xFFu) / 255.0,  // g
                float((color >>  8) & 0xFFu) / 255.0,  // b
                float((color      ) & 0xFFu) / 255.0); // a
}

void main() {
    uvec2 pixel = gl_GlobalInvocationID.xy;
    if (pixel.x >= screenWidth || pixel.y >= screenHeight) return;

    vec4 worldPosPack = imageLoad(inputWorldPos, ivec2(pixel * SSR_RES));
    vec3 worldPos = worldPosPack.xyz;
    vec4 specColor = imageLoad(inputSpecular, ivec2(pixel * SSR_RES));
    vec4 normalPack = unpackColor32(floatBitsToUint(worldPosPack.a));
    vec3 normal = vec3((normalPack.r * 2.0) - 1.0,(normalPack.g * 2.0) - 1.0,(normalPack.b * 2.0) - 1.0);
    normal = normalize(normal);
    vec3 viewDir = normalize(camPos - worldPos);
    vec3 reflectDir = reflect(-viewDir, normal);
    float cosTheta = max(dot(-viewDir, normal), 0.0);
    vec2 hitUV = vec2(0.0);
    vec3 reflectionColor = vec3(0.0);
    float totalWeight = 0.0;
    vec3 marchPos = worldPos + normal * 0.005;
    float traveled = 0.0;
    vec3 rayStep = reflectDir * stepSize;
    vec2 screenSize = vec2(screenWidth, screenHeight);
    ivec2 screenSizeLess1 = ivec2(screenWidth - 1, screenHeight - 1);
    ivec2 zeroIVec2 = ivec2(0, 0);
    float confidence = 0.0;
    for (int i = 0; i < stepCount; ++i) {
        marchPos += rayStep;
        traveled += stepSize;
        vec4 clipPos = viewProjection * vec4(marchPos, 1.0);
        vec2 ndc = clipPos.xy / clipPos.w;
        hitUV = ndc * 0.5 + 0.5;
        if (hitUV.x <= 0.05 || hitUV.x >= 0.95 || hitUV.y <= 0.05 || hitUV.y >= 0.95) break;

        ivec2 samplePixel = ivec2(hitUV * screenSize);
        samplePixel = clamp(samplePixel, zeroIVec2, screenSizeLess1);
        vec4 sampledWorldPosPack = imageLoad(inputWorldPos, samplePixel * SSR_RES);
        vec3 sampledWorldPos = sampledWorldPosPack.xyz;
        vec4 sampledNormalPack = unpackColor32(floatBitsToUint(sampledWorldPosPack.a));
        vec3 sampledNormal = normalize(vec3(sampledNormalPack.r * 2.0 - 1.0, sampledNormalPack.g * 2.0 - 1.0, sampledNormalPack.b * 2.0 - 1.0));
        sampledNormal = normalize(sampledNormal);
        if (dot(reflectDir, sampledNormal) >= 0.0) continue;
        
        float dist2 = dot(marchPos - sampledWorldPos, marchPos - sampledWorldPos);
        float weight = exp(-dist2 / sampleWeight);
        if (weight > 0.01) { // Minimum weight threshold
            ivec2 hitPixel = ivec2(hitUV * vec2(screenWidth, screenHeight));
            hitPixel = clamp(hitPixel, ivec2(0), ivec2(int(screenWidth)-1, int(screenHeight)-1));
            vec4 sampleColor = imageLoad(inputImage, hitPixel * SSR_RES);
            reflectionColor += sampleColor.rgb * weight;
            totalWeight += weight;
            confidence += weight;
            if (totalWeight > 0.8) break;
        }
    }

    if (totalWeight < 0.0001) { imageStore(outputImage, ivec2(pixel), vec4(0.0,0.0,0.0,1.0)); return; } // Avoid division by near-zero

    reflectionColor /= totalWeight; // Normalize by total weight
    vec3 combined = mix(vec3(0.0,0.0,0.0), reflectionColor, vec3(1.0) * 0.451); // Just so happens to work well with, yes, another magical 451
    vec3 prev = imageLoad(outputImage, ivec2(pixel)).rgb;
    vec3 result = mix(prev, combined, confidence > 0.1 ? 0.9 : 0.1);
    imageStore(outputImage, ivec2(pixel), vec4(result, 1.0));
}
